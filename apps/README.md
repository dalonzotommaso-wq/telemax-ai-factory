# Applications

Runnable applications built on the framework (for example a CLI or an HTTP service). Each application lives in its own subdirectory as a workspace package (`apps/*`) and depends on `@telemax/core` and the feature packages it needs.

> This directory is part of the foundation scaffold and is intentionally empty
> for now. It contains no example or demo code.
