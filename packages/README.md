# Packages

Feature packages and SDKs published under the `@telemax/*` scope (`packages/*`). Generators and other capabilities live here. Every package must depend on `@telemax/core` — never the other way around — and must have a single, well-defined responsibility.

> This directory is part of the foundation scaffold and is intentionally empty
> for now. It contains no example or demo code.
