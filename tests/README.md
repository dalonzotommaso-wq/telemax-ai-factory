# Integration Tests

Cross-package integration and end-to-end tests that exercise several packages together. Unit tests live next to the code they cover, inside each package's `src/` as `*.test.ts` files.

> This directory is part of the foundation scaffold and is intentionally empty
> for now. It contains no example or demo code.
