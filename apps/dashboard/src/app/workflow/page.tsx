import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function Page() {
  return (
    <PagePlaceholder
      title="Workflow"
      description="Workflow definitions and runs executed by the Workflow Engine."
    />
  );
}
