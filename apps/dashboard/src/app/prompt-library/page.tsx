import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function Page() {
  return (
    <PagePlaceholder
      title="Prompt Library"
      description="Reusable prompt templates managed by the Prompt Engine."
    />
  );
}
