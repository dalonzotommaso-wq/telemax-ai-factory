# Prompts

The prompt library: versioned, reviewable prompt definitions used by AI-backed generators. Keeping prompts here makes them first-class, diffable artifacts rather than strings buried in code.

> This directory is part of the foundation scaffold and is intentionally empty
> for now. It contains no example or demo code.
