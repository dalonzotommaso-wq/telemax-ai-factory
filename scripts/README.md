# Scripts

Repository automation scripts (maintenance, verification, release helpers) invoked via `pnpm` tasks. Scripts here operate on the repository itself and are not shipped in any published package.

> This directory is part of the foundation scaffold and is intentionally empty
> for now. It contains no example or demo code.
